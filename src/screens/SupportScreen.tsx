import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const SupportScreen = () => {
    return (
        <SafeAreaView style={{ flex: 1, justifyContent: 'center' }}>
            <Text style={{ textAlign: 'center' }}>Support Screen</Text>
        </SafeAreaView>
    );
};

export default SupportScreen;